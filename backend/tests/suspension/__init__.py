"""Regression matrix for tasks 2.1-2.5 of ``suspend-fingerprint-integration`` PR 2.

Split into focused modules (see ``test_canonical_gates``,
``test_legacy_routes``, ``test_agent_mutations``, ``test_payload_redaction``,
``test_conversion_split``, ``test_tablet_under_suspension`` and
``test_flag_off_rollback``) so reviewers can read each behaviour group
independently.
"""
