"""Backup service (dump + retention + lock + audit).

Filled in by the Phase 2 commit. The module is kept import-clean
during bootstrap so the app config loads cleanly.
"""

from __future__ import annotations

__all__: list[str] = []