# Pinned to the exact minor versions the agent was tested against.
# The hardware stack (dbus-python, PyGObject) is installed via apt
# on the production PC (see README §"Install"). Putting those in
# requirements.txt would force a C build on every pip install.

# --- HTTP stack ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
structlog==24.4.0
httpx==0.27.2

# --- Test stack ---
pytest==8.3.0
pytest-asyncio==0.24.0

# --- Hardware stack (apt-installed on Ubuntu 24.04) ---
# The agent's :mod:`agent.fprintd_bridge` module imports dbus and
# PyGObject lazily so the pytest suite can run on machines without
# the hardware. On the admin PC, install via:
#   sudo apt install python3-dbus python3-gi libfprint-2-2 fprintd
# dbus-python==1.3.2
# PyGObject==3.50.0
